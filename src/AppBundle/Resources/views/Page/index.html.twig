{% extends "::base.html.twig"%}
{% block title %}
         Karavarum
{% endblock %}
{% block body %}
    <ul>
        {% for category in categories %}
            <a href="{{ path('categorySlug', {'slug':category.categorySlug}) }}">{{ category.name }}</a>
        {% endfor %}
    </ul>
{% endblock %}